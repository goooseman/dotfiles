stream {
    upstream ssh {
        server localhost:22;
    }
    server {
        listen        80;
        server_name ssh.raspberrypi.local ssh.home.goooseman.ru;
        proxy_pass    ssh;
    } 
}